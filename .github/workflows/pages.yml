name: GitHub Pages 部署

# 镜像托管仅为备份：不随 push 或审批自动更新，仅在需要刷新时
# 于 Actions 页手动触发（Run workflow）。主站在 Cloudflare Pages。
on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

#  deployments 互相覆盖无意义，取消进行中的旧部署
concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      # repo_url 已由数据文件指向本仓库，无需 REPO_URL 覆盖；
      # SITE_URL 走仓库 Actions Variable，未配置时回退数据文件的 site_url
      - name: 构建静态页
        env:
          SITE_URL: ${{ vars.SITE_URL }}
        run: node build.js

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
